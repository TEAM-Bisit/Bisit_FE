<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="@drawable/bg_card_white">

    <TextView
        android:id="@+id/tvSortTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/sort_title"
        android:textColor="#222222"
        android:textStyle="bold"
        android:textSize="16sp"
        android:letterSpacing="-0.02"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp" />

    <TextView
        android:id="@+id/optionRecent"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="38dp"
        android:gravity="center_vertical"
        android:text="@string/sort_recent"
        android:textSize="14sp"
        android:textColor="#222222"
        android:letterSpacing="-0.02"
        android:clickable="true"
        android:focusable="true"
        app:drawableEndCompat="@drawable/ic_check_red"
        tools:ignore="TouchTargetSizeCheck" />

    <TextView
        android:id="@+id/optionOldest"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="38dp"
        android:gravity="center_vertical"
        android:text="@string/sort_oldest"
        android:textSize="14sp"
        android:textColor="#222222"
        android:letterSpacing="-0.02"
        android:clickable="true"
        android:focusable="true"
        app:drawableEndCompat="@drawable/ic_check_gray"
        tools:ignore="TouchTargetSizeCheck" />

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/btnSave"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_marginTop="8dp"
        android:text="@string/sort_save"
        android:textColor="@android:color/white"
        android:textSize="14sp"
        android:background="@drawable/bg_button_blue"
        android:stateListAnimator="@null"
        android:gravity="center"
        tools:ignore="TextContrastCheck" />

</LinearLayout>
